AWSTemplateFormatVersion: "2010-09-09"
Description: chibiharu's Qiita [【AWS】CloudFormationでWebサーバを構築③（RDS編）] - DatabaseTemplate


# ------------------------------------------------------------#
# Input Parameters
# ------------------------------------------------------------# 
Parameters:
### Project Prefix ###
  PJPrefix:
    Type: String
### RDS Name ###
  chibiDBEngineName:
    Type: String
    Default: "QiitaWebEnvRDSForMySQL"
### RDS Database type & Major version ###
  chibiMySQLMajorVersion:
    Type: String
    Default: "5.7"
    AllowedValues: [ "5.5", "5.6", "5.7" ]
### RDS Database type & Minor version ###
  chibiMySQLMinorVersion:
    Type: String
    Default: "22"
### Instnce Options ###
  DBInstanceClass:
    Type: String
    Default: "db.m4.large" 
  chibiDBEngineStorageSize:
    Type: String
    Default: "30"
  chibiDBEngineStorageType:
    Type: String
    Default: "gp2"
### Database name ###
  DBName:
    Type: String
    Default: "QiitaWebEnvDB"
### User Options ###
  chibiDBMasterUserName:
    Type: String
    Default: "dbuser"
    NoEcho: true
    MinLength: 1
    MaxLength: 16
    AllowedPattern: "[a-zA-Z][a-zA-Z0-9]*"
    ConstraintDescription: "must begin with a letter and contain only alphanumeric characters."
  chibiDBPassword: 
    Default: "DBPassword"
    NoEcho: true
    Type: String
    MinLength: 8
    MaxLength: 41
    AllowedPattern: "[a-zA-Z0-9]*"
    ConstraintDescription: "must contain only alphanumeric characters."
### AZ options ###
  MultiAZ: 
    Default: "false"
    Type: String
    AllowedValues: [ "true", "false" ]


### Resources ###
Resources: 
# ------------------------------------------------------------#
#  DB - Engine
# ------------------------------------------------------------#
  chibiDBEngine: 
    Type: "AWS::RDS::DBInstance"
    Properties: 
      DBInstanceIdentifier: !Sub "${chibiDBEngineName}"
      Engine: MySQL
      EngineVersion: !Sub "${chibiMySQLMajorVersion}.${chibiMySQLMinorVersion}"
      DBInstanceClass: !Ref DBInstanceClass
      AllocatedStorage: !Ref chibiDBEngineStorageSize
      StorageType: !Ref chibiDBEngineStorageType
      DBName: !Ref DBName
      MasterUsername: !Ref chibiDBMasterUserName
      MasterUserPassword: !Ref chibiDBPassword
      DBSubnetGroupName: !Ref chibiDBSubnetGroup
      PubliclyAccessible: false
      MultiAZ: !Ref MultiAZ
      PreferredBackupWindow: "18:00-18:30"
      PreferredMaintenanceWindow: "sat:19:00-sat:19:30"
      AutoMinorVersionUpgrade: false
      DBParameterGroupName: !Ref chibiDBParameterGroup  
      VPCSecurityGroups:
        - { "Fn::ImportValue": !Sub "${PJPrefix}-db-sg" }
      CopyTagsToSnapshot: true
      BackupRetentionPeriod: 7
      Tags: 
        - Key: "Name"
          Value: !Ref chibiDBEngineName
    DeletionPolicy: "Delete"
    
    
# ------------------------------------------------------------#
#  DB - SubnetGroup
# ------------------------------------------------------------#
  chibiDBSubnetGroup: 
    Type: "AWS::RDS::DBSubnetGroup"
    Properties: 
      DBSubnetGroupName: !Sub "${chibiDBEngineName}-subnet"
      DBSubnetGroupDescription: "-"
      SubnetIds: 
        - { "Fn::ImportValue": !Sub "${PJPrefix}-private-subnet-a" }
        - { "Fn::ImportValue": !Sub "${PJPrefix}-private-subnet-c" }


# ------------------------------------------------------------#
#  DB - ParameterGroup
# ------------------------------------------------------------#
  chibiDBParameterGroup:
    Type: "AWS::RDS::DBParameterGroup"
    Properties:
      Family: !Sub "MySQL${chibiMySQLMajorVersion}"
      Description: !Sub "${chibiDBEngineName}-parm"
      

# ------------------------------------------------------------#
# Output Parameters
# ------------------------------------------------------------#                
Outputs:

### DBEngine ###
  chibiDBEngine:
    Value: !Ref chibiDBEngine
    Export:
      Name: !Sub "${chibiDBEngineName}-id"
      
### DBEndopoint ###
  DBInstanceEndpoint:
    Value: !GetAtt chibiDBEngine.Endpoint.Address
    Export:
      Name: !Sub "${chibiDBEngineName}-endpoint"
      
### DBName ###
  DBName:
    Value: !Ref DBName
    Export:
      Name: !Sub "${chibiDBEngineName}-DBName"


